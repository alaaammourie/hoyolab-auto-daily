name: Verify Secrets

on:
  workflow_dispatch:  # Allows manual trigger
  push:
    branches: [ main, develop ]

jobs:
  verify-secrets:
    runs-on: ubuntu-latest
    name: Check Required Secrets
    
    steps:
    - name: Print secret values (DANGER - use only for debugging)
      run: |
        echo "⚠️  WARNING: Printing secret values in logs!"
        echo "🚨 SECURITY RISK: Anyone with repo access can see these!"
        echo "================================================"
        
        echo "COOKIE value:"
        echo "$COOKIE"
        echo "================================================"
        echo "⚠️  Remember to remove this step after debugging!"
      env:
        COOKIE: ${{ secrets.COOKIE }}